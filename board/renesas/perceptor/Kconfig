if TARGET_PERCEPTOR

config SYS_SOC
        default "renesas"

config SYS_BOARD
        default "perceptor"

config SYS_VENDOR
        default "renesas"

config SYS_CONFIG_NAME
        default "perceptor"

endif
